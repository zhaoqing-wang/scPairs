% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{scpairs_testdata}
\alias{scpairs_testdata}
\title{Synthetic Seurat Test Object for scPairs Examples and Tests}
\format{
A \code{\link[SeuratObject:Seurat-class]{Seurat}} object with:
\describe{
\item{Assay RNA}{
\itemize{
\item \strong{counts}: raw integer count matrix (20 genes × 100 cells).
\item \strong{data}: log-normalised expression matrix.
\item \strong{scale.data}: z-score–scaled matrix for variable features.
}
}
\item{Reductions}{
\itemize{
\item \strong{pca}: 5-component principal-component embedding.
\item \strong{umap}: 2-D UMAP embedding derived from the top 5 PCs.
}
}
\item{Metadata}{
\itemize{
\item \strong{seurat_clusters}: factor with three balanced cluster
labels (\code{"1"}, \code{"2"}, \code{"3"}).
}
}
\item{Genes}{\code{GENE1}–\code{GENE20} (synthetic).}
\item{Cells}{\code{CELL001}–\code{CELL100}.}
}
}
\source{
Generated by \code{data-raw/make_testdata.R} with
\code{set.seed(7391)}.
}
\usage{
data(scpairs_testdata)
}
\description{
A minimal synthetic Seurat object intended for use in package examples and
unit tests.  It contains 100 cells and 20 genes with deliberately injected
co-expression patterns, allowing users to verify that scPairs functions run
correctly and return meaningful results without requiring real single-cell
data.

The dataset ships with normalised expression, variable-feature selection,
scaled data, a 5-component PCA, and a 2-D UMAP embedding, so it can be
passed directly to every scPairs discovery and visualisation function.
}
\details{
Two co-expression patterns were injected during data generation:
\itemize{
\item \strong{GENE3 & GENE4}: strongly correlated across all cells
(Pearson r ≈ 0.89 in the normalised data).  These are the recommended
genes to use in discovery and assessment examples.
\item \strong{GENE1 & GENE2}: moderately correlated within cluster 1
only (cluster-specific pattern).
\item All remaining gene pairs are near-independent noise.
}
The data were generated with a fixed random seed so the object is fully
reproducible.  The generation script is provided in \code{data-raw/make_testdata.R}.
}
\examples{
# Load and inspect the object
data(scpairs_testdata)
scpairs_testdata

# Quick co-expression check on the injected pair
norm <- SeuratObject::LayerData(scpairs_testdata, layer = "data")
cor(as.numeric(norm["GENE3", ]), as.numeric(norm["GENE4", ]))

}
\keyword{datasets}
