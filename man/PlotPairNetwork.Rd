% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network.R
\name{PlotPairNetwork}
\alias{PlotPairNetwork}
\title{Plot Gene Interaction Network}
\usage{
PlotPairNetwork(
  result,
  top_n = 50,
  min_score = 0,
  confidence = NULL,
  layout = "fr",
  node_color = "#2C3E50",
  edge_palette = c(High = "#E74C3C", Medium = "#F39C12", Low = "#95A5A6", NS = "#D5D8DC"),
  label_size = 3.5,
  title = NULL,
  show_legend = TRUE
)
}
\arguments{
\item{result}{An object of class \code{"scPairs_result"}, \code{"scPairs_gene_result"},
or a \code{data.frame} / \code{data.table} with columns \code{gene1}, \code{gene2},
\code{synergy_score}.}

\item{top_n}{Integer; show only the top N edges.  Default 50.}

\item{min_score}{Numeric; minimum synergy score to display an edge.}

\item{confidence}{Character vector; filter to these confidence levels
(e.g., \code{c("High", "Medium")}).  NULL = no filter.}

\item{layout}{Character; ggraph layout algorithm.  Default \code{"fr"}
(Fruchterman-Reingold).}

\item{node_color}{Character; colour for nodes.  Default \code{"#2C3E50"}.}

\item{edge_palette}{Character vector of 3 colours for confidence
(High, Medium, Low).  Default blue-orange-grey scheme.}

\item{label_size}{Numeric; node label font size.}

\item{title}{Character; plot title.}

\item{show_legend}{Logical.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a publication-ready gene interaction network from scPairs results.
Nodes represent genes; edges represent synergistic relationships.  Edge
width encodes synergy score; edge colour encodes confidence.  Node size
optionally reflects the number of significant partners (degree centrality).
}
\examples{
\dontrun{
result <- FindAllPairs(seurat_obj, top_n = 200)
PlotPairNetwork(result, top_n = 50)

# For a gene query
gene_result <- FindGenePairs(seurat_obj, gene = "CD3D")
PlotPairNetwork(gene_result, top_n = 20)
}

}
